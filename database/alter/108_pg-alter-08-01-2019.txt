SET SCHEMA 'zafira';

select check_version(108);

ALTER TABLE LAUNCHERS ADD COLUMN JOB_ID INT NULL;

ALTER TABLE LAUNCHERS
   ADD CONSTRAINT fk_LAUNCHERS_JOBS1
    FOREIGN KEY (JOB_ID)
    REFERENCES JOBS (ID)
    ON DELETE CASCADE
    ON UPDATE NO ACTION;

INSERT INTO SETTINGS (NAME, VALUE, TOOL) VALUES ('JENKINS_LAUNCHER_JOB_NAME', 'launcher', 'JENKINS');

select add_version(108);
