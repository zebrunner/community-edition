SET SCHEMA 'zafira';

select check_version(113);

ALTER TABLE GROUPS ADD COLUMN INVITABLE BOOLEAN NOT NULL DEFAULT TRUE;

DO $$
  DECLARE GROUP_ID GROUPS.id%TYPE;

  BEGIN

    SELECT ID INTO GROUP_ID FROM GROUPS WHERE NAME = 'Super admins';
    UPDATE GROUPS SET INVITABLE = FALSE WHERE ID = GROUP_ID;

  END$$;

select add_version(113);
