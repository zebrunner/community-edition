SET SCHEMA 'zafira';

select check_version(122);

DROP TABLE MONITOR_STATUSES CASCADE;
DROP TABLE MONITORS;

DELETE FROM GROUP_PERMISSIONS WHERE PERMISSION_ID IN (SELECT ID FROM PERMISSIONS WHERE BLOCK = 'MONITORS');
DELETE FROM PERMISSIONS WHERE BLOCK = 'MONITORS';

select add_version(122);
