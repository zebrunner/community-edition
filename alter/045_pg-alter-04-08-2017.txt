set schema 'zafira';


-- Phase 1
DELETE FROM TEST_ARTIFACTS WHERE ID NOT IN ( SELECT MAX(ID) FROM TEST_ARTIFACTS GROUP BY TEST_ID, NAME HAVING COUNT (*) > 1);

-- Phase 2
CREATE UNIQUE INDEX NAME_TEST_ID_UNIQUE ON TEST_ARTIFACTS (NAME, TEST_ID);

